# Android-only env vars (used by the Android build).
# Copy to `.env.android` and fill in real values.

# MOBILE_API_KEY: Required for Android app API calls (sent as X-API-KEY)
MOBILE_API_KEY=replace_with_the_same_key_as_render

